# Django settings
SECRET_KEY=pon-tu-clave-secreta-aqui
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos (ejemplo para SQLite, para otros motores agrega más variables)
DB_ENGINE=django.db.backends.sqlite3
# Usar ruta absoluta para evitar confusión entre múltiples db.sqlite3
DB_NAME=C:\Users\Public\Documents\facultad\3er Año\Trabajo Final\proyecto_imprenta\proyec_imprenta\imprenta_tucan\db.sqlite3

# Email (SMTP Gmail) - generado automáticamente (placeholders)
# Anymail (API) - SOLO SES
ANYMAIL_PROVIDER=ses
ANYMAIL_API_KEY=TU_API_KEY_AQUI
# Para Mailgun solo si usas mailgun
ANYMAIL_DOMAIN=tu-dominio.mailgun.org
# Para SES (usa tus credenciales AWS y región)
AWS_REGION=us-east-1

# Opcional: backend de fallback si no hay provider (console/filebased/smtp)
EMAIL_BACKEND=anymail.backends.amazon_ses.EmailBackend
DEFAULT_FROM_EMAIL=no-reply@imprenta.local
